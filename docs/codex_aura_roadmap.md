# 🗺️ Дорожная Карта Codex Aura (MCP) v2.0

---

## Фаза 0: 🧪 Proof of Concept (2-3 недели)

> **Цель**: Валидация идеи и базовой архитектуры

### Ключевой Результат
Работающий CLI-прототип, демонстрирующий ценность графового подхода.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **CLI Tool** | Принимает путь к репозиторию, выводит JSON-граф | P0 |
| **Python Analyzer** | Только Python, только `IMPORTS` | P0 |
| **JSON Schema** | Спецификация формата графа v0.1 | P0 |
| **Demo Script** | Скрипт, показывающий "affected files" для изменения | P0 |

### Технические Решения
- Язык: Python 3.11+
- Парсер: `ast` (стандартная библиотека) → простота
- Формат выхода: JSON файл
- Тестирование на 2-3 open source репозиториях

### Критерии Завершения
- [ ] CLI генерирует корректный граф для Flask-приложения (~50 файлов)
- [ ] Демо показывает: "изменили X.py → затронуты Y.py, Z.py"
- [ ] Время анализа < 10 секунд для 10K LOC

---

## Фаза 1: 🏗️ Open Source MVP (1.5-2 месяца)

> **Цель**: Публичный релиз ядра для привлечения community

### Ключевой Результат
**Codex Aura Core** — open source библиотека + CLI с базовым HTTP API.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **MCP Protocol Spec v1.0** | Публичная спецификация (GitHub Wiki / docs site) | P0 |
| **Core Analyzer** | Python + `IMPORTS`, `CALLS`, `EXTENDS` | P0 |
| **Graph Storage** | SQLite для локального хранения (zero dependencies) | P0 |
| **HTTP Server** | FastAPI, базовые эндпоинты | P0 |
| **VS Code Extension v0.1** | Read-only визуализация графа | P1 |
| **GitHub Action** | Автоматическая генерация графа в CI | P1 |

### API Эндпоинты (MVP)

```
GET  /api/v1/health
GET  /api/v1/graph/stats
GET  /api/v1/graph/node/{id}
GET  /api/v1/graph/dependencies?file={path}
GET  /api/v1/impact?files={paths}
```

### Спецификация Протокола v1.0

**Формат узла (Node):**
```json
{
  "id": "src/auth/jwt.py::validate_token",
  "type": "function",
  "name": "validate_token",
  "path": "src/auth/jwt.py",
  "lines": [45, 89],
  "metadata": {
    "sha": "abc123",
    "description": "Validates JWT token and returns claims",
    "complexity": 8
  }
}
```

**Формат ребра (Edge):**
```json
{
  "source": "src/auth/jwt.py::validate_token",
  "target": "src/utils/crypto.py::decode_jwt",
  "type": "CALLS",
  "metadata": {
    "line": 52
  }
}
```

### Open Source
- **Лицензия**: Apache 2.0
- **Репозиторий**: GitHub с CI/CD, contributing guide
- **Документация**: MkDocs или Docusaurus
- **Package**: PyPI (`pip install codex-aura`)

### Критерии Завершения
- [ ] 100+ GitHub stars (vanity, но важно для adoption)
- [ ] 3+ внешних контрибьютора
- [ ] Документация покрывает все API
- [ ] Работает на реальных проектах: Django, FastAPI, Flask

---

## Фаза 2: 🚀 Коммерческий Сервер (2-3 месяца)

> **Цель**: Запуск SaaS с монетизацией

### Ключевой Результат
**Codex Aura Cloud** — managed сервис с уникальными premium-функциями.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **Neo4j Integration** | Графовая БД для production | P0 |
| **Webhook Handler** | GitHub/GitLab webhooks, инкрементальные апдейты | P0 |
| **Semantic Search** | Vector DB + embeddings для `/context` | P0 |
| **Smart Context API** | POST `/context` с token budgeting | P0 |
| **Dashboard** | Web UI для управления репозиториями | P1 |
| **Billing System** | Stripe интеграция | P1 |
| **Telemetry** | Анонимная аналитика использования | P1 |

### Premium Features (Closed Source)

| Функция | Описание | Ценность |
|---------|----------|----------|
| **Semantic Context** | LLM-powered выбор релевантного контекста | Главная ценность |
| **Token Budgeting** | Приоритизация в рамках лимита | Cost savings |
| **Incremental Sync** | < 5 сек обновления | Real-time актуальность |
| **Impact Analysis** | Предсказание затронутых файлов | Code review quality |
| **Multi-repo** | Связи между репозиториями | Микросервисы |
| **Historical Graphs** | Граф для любого SHA | Debugging, audit |

### Архитектура Cloud

```
┌─────────────────────────────────────────────────────────────┐
│                     Load Balancer                           │
└─────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
     ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
     │  API Pod 1  │ │  API Pod 2  │ │  API Pod N  │
     └──────┬──────┘ └──────┬──────┘ └──────┬──────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
 ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
 │   Neo4j     │     │   Redis     │     │   Qdrant    │
 │  Cluster    │     │   Cluster   │     │  (Vectors)  │
 └─────────────┘     └─────────────┘     └─────────────┘
        │
        ▼
 ┌─────────────┐
 │ PostgreSQL  │  (Users, Billing, Audit)
 └─────────────┘
```

### Модель Монетизации

| План | Цена | Включено |
|------|------|----------|
| **Free** | $0 | 1 репо (public), 100 запросов/месяц |
| **Pro** | $29/мес | 5 репо, 10K запросов, private repos |
| **Team** | $99/мес | 20 репо, 50K запросов, team features |
| **Enterprise** | Custom | Unlimited, self-hosted, SLA, SSO |

### Критерии Завершения
- [ ] 10+ платящих клиентов
- [ ] 99.9% uptime за месяц
- [ ] P95 latency `/context` < 500ms
- [ ] Положительные отзывы от AI-agent разработчиков

---

## Фаза 3: 🌐 Языковое Расширение (2-3 месяца)

> **Цель**: Поддержка enterprise-стека языков

### Ключевой Результат
Полная поддержка JavaScript/TypeScript и Java.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **TypeScript Analyzer** | LSP-based через tsserver | P0 |
| **JavaScript Analyzer** | Включая JSX/React | P0 |
| **Java Analyzer** | LSP через Eclipse JDT | P1 |
| **Go Analyzer** | gopls интеграция | P2 |
| **Multi-language Repos** | Корректная работа в полиглот-репо | P0 |

### LSP Integration Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Codex Aura Analyzer                   │
├─────────────────────────────────────────────────────────┤
│                    LSP Bridge Layer                     │
│  ┌─────────────────────────────────────────────────┐   │
│  │  • Initialize LSP server per language           │   │
│  │  • Send textDocument/definition requests        │   │
│  │  • Send textDocument/references requests        │   │
│  │  • Parse responses into Graph edges             │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │ Pyright  │  │ tsserver │  │   JDT    │  │ gopls  │  │
│  │ (Python) │  │   (TS)   │  │  (Java)  │  │  (Go)  │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘  │
└─────────────────────────────────────────────────────────┘
```

### Расширенные Типы Рёбер

| Язык | Новые типы рёбер |
|------|------------------|
| TypeScript | `IMPLEMENTS`, `USES_TYPE`, `EXPORTS` |
| React | `RENDERS` (компонент рендерит компонент) |
| Java | `ANNOTATED_BY`, `THROWS` |
| All | `TESTS` (связь тест → тестируемый код) |

### Критерии Завершения
- [ ] TypeScript analyzer работает на Next.js приложении
- [ ] Java analyzer работает на Spring Boot проекте
- [ ] Cross-language imports корректно отображаются
- [ ] Документация для каждого языка

---

## Фаза 4: 🏢 Enterprise Features (3-4 месяца)

> **Цель**: Готовность к enterprise adoption

### Ключевой Результат
Self-hosted вариант, SSO, advanced integrations.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **Self-Hosted Package** | Docker Compose / Helm chart | P0 |
| **SSO Integration** | SAML, OIDC (Okta, Azure AD) | P0 |
| **Audit Logging** | Полный audit trail | P0 |
| **RBAC** | Role-based access control | P0 |
| **Jira Integration** | Bi-directional sync | P1 |
| **GitHub Enterprise** | On-prem GitHub support | P1 |
| **Compliance** | SOC2 Type II certification | P2 |

### CI/CD Integrations

```yaml
# GitHub Actions Example
- name: Codex Aura Impact Check
  uses: codex-aura/impact-action@v1
  with:
    api_key: ${{ secrets.CODEX_AURA_KEY }}
    fail_on_risk: high
    comment_on_pr: true
```

**Функционал:**
- Автоматический комментарий в PR с impact analysis
- Блокировка merge при высоком риске
- Предложение тестов для запуска
- Badge со статусом покрытия графа

### Интеграция с Таск-Трекерами

| Трекер | Функционал |
|--------|------------|
| **Jira** | Автосвязь коммитов с тикетами, поле "Affected Files" |
| **Linear** | Webhook sync, граф в sidebar тикета |
| **GitHub Issues** | Автолинк через commit messages |

### Advanced Analytics Dashboard

- **Dependency Health Score** — оценка здоровья зависимостей
- **Hotspot Map** — визуализация часто меняющегося кода
- **Architecture Drift Detection** — алерты при нарушении boundaries
- **Onboarding Report** — автогенерация документации для новичков

### Критерии Завершения
- [ ] 3+ enterprise клиента на self-hosted
- [ ] SOC2 Type I certification
- [ ] SLA 99.95% для enterprise плана
- [ ] < 30 минут на установку self-hosted

---

## Фаза 5: 🧠 Code Intelligence Layer (2-3 месяца)

> **Цель**: Проверка кода на соответствие стилю, паттернам и бизнес-логике

### Ключевой Результат
Validation API, который проверяет сгенерированный код до его применения.

### Deliverables

| Компонент | Описание | Приоритет |
|-----------|----------|-----------|
| **Style Guide Extractor** | Автоизвлечение code style из репозитория | P0 |
| **Pattern Detector** | Обнаружение архитектурных паттернов | P0 |
| **Business Rules Engine** | Хранение и проверка бизнес-правил | P0 |
| **Validation API** | POST `/validate` для проверки кода | P0 |
| **Duplicate Detector** | Поиск похожего существующего кода | P1 |
| **Rules UI** | Интерфейс для управления правилами | P1 |
| **Auto-extraction** | AI-извлечение правил из кода/тестов | P2 |

### Style Guide Extractor

**Автоматически извлекает:**
```
┌─────────────────────────────────────────────┐
│  📊 Анализ 1000+ функций в репозитории      │
│                                             │
│  Naming:     snake_case (98.5%)             │
│  Docstrings: Google style (95%)             │
│  Imports:    Absolute, grouped (100%)       │
│  Types:      Always with hints (87%)        │
│  Errors:     Custom exceptions (92%)        │
└─────────────────────────────────────────────┘
```

### Pattern Library

**Обнаруживаемые паттерны:**
- Repository Pattern (data access)
- Service Layer (business logic)
- Factory Pattern (object creation)
- DTO/Schema (data transfer)
- Event-driven (emit/handle)
- Middleware (pre/post processing)

### Business Rules Engine

**Источники правил:**

| Источник | Метод извлечения |
|----------|------------------|
| Код | Анализ валидаций, assertions, guards |
| Комментарии | Парсинг `# RULE:`, `# BUSINESS:` |
| Тесты | Анализ test case names и assertions |
| Документы | NLP по README, Wiki, Confluence |
| Ручной ввод | API / UI для аналитиков |

**Формат правила:**
```yaml
- id: BR-001
  name: Order Cancellation
  condition: order.status NOT IN ['shipped', 'delivered']
  applies_to: [OrderService.cancel_order]
  severity: error
```

### Validation API

```http
POST /api/v1/validate
{
  "repo_id": "xxx",
  "code": "def cancel_order(...)...",
  "checks": ["style", "patterns", "rules", "duplicates"]
}
```

**Response:**
```json
{
  "valid": false,
  "score": 0.45,
  "issues": [
    {
      "type": "business_rule_violation",
      "rule_id": "BR-001",
      "message": "Missing status check before cancellation",
      "fix_example": "if order.status in ['shipped']:\n    raise ..."
    }
  ]
}
```

### Критерии Завершения
- [ ] Style Guide извлекается автоматически
- [ ] 5+ паттернов обнаруживаются
- [ ] Validation API работает < 2 сек
- [ ] UI для управления правилами
- [ ] Интеграция с Agent SDK

---

## Фаза 6: 🤖 AI-Native Features (Ongoing)

> **Цель**: Максимальная ценность для AI-агентов

### Deliverables

| Компонент | Описание |
|-----------|----------|
| **Agent SDK** | Python/JS SDK для интеграции с агентами |
| **Feedback Loop** | Агенты сообщают, какой контекст был полезен |
| **Auto-enrichment** | LLM дополняет граф семантическими связями |
| **"Explain Codebase"** | Генерация onboarding-документации |
| **Cross-repo Analysis** | Связи между микросервисами |
| **Predictive Context** | Предсказание нужного контекста до запроса |

### Agent SDK Example

```python
from codex_aura import CodexAuraClient

client = CodexAuraClient(api_key="...")

# Получить контекст для задачи
context = client.get_context(
    repo="myorg/myrepo",
    task="Fix authentication bug in JWT validation",
    task_type="bug_fix",
    max_tokens=8000
)

# Использовать в промпте для LLM
prompt = f"""
You are fixing a bug. Here is the relevant code context:

{context.to_prompt()}

The bug report: {bug_description}

Please provide a fix.
"""

# После выполнения — feedback
client.submit_feedback(
    context_id=context.id,
    used_files=["src/auth/jwt.py", "src/utils/crypto.py"],
    unused_files=["src/models/user.py"],
    rating=4
)
```

### Критерии Завершения
- [ ] SDK используется 5+ AI-agent проектами
- [ ] Feedback loop улучшает precision на 10%+
- [ ] "Explain Codebase" генерирует полезную документацию

---

## 📅 Timeline Overview

```
      2024 Q4        2025 Q1        2025 Q2        2025 Q3        2025 Q4
   ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐
   │  Phase 0  │  │  Phase 1  │  │  Phase 2  │  │  Phase 3  │  │  Phase 4  │
   │    PoC    │  │  OS MVP   │  │   Cloud   │  │ Languages │  │Enterprise │
   │  2-3 wks  │  │  6-8 wks  │  │ 8-12 wks  │  │ 8-12 wks  │  │ 12-16 wks │
   └───────────┘  └───────────┘  └───────────┘  └───────────┘  └───────────┘
                                                                     │
                                       ┌─────────────────────────────┘
                                       ▼
                              ┌─────────────────────────────────────────┐
                              │  Phase 5: Code Intelligence (8-12 wks) │
                              │  • Style Guide Extraction              │
                              │  • Pattern Detection                   │
                              │  • Business Rules Engine               │
                              │  • Validation API                      │
                              ├─────────────────────────────────────────┤
                              │  Phase 6: AI-Native Features (ongoing) │
                              └─────────────────────────────────────────┘
```

---

## 🎯 Success Metrics (North Stars)

| Метрика | Phase 1 | Phase 2 | Phase 4 | Phase 5 |
|---------|---------|---------|---------|---------|
| GitHub Stars | 100 | 500 | 2000 | 3000 |
| Active Users (monthly) | 50 | 500 | 5000 | 10000 |
| Paying Customers | 0 | 10 | 100 | 200 |
| MRR | $0 | $1K | $50K | $100K |
| Repos Indexed | 10 | 500 | 10,000 | 25,000 |
| API Calls / month | 1K | 100K | 10M | 50M |
| Agent Code Accuracy | — | — | — | >70% |
| Style Compliance | — | — | — | >95% |

---

## 🚨 Риски и Митигации

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| LSP серверы нестабильны | Средняя | Высокое | Fallback на AST-парсеры |
| Низкий adoption open source | Средняя | Высокое | Активный DevRel, интеграции |
| Конкуренция (Sourcegraph, Code Wiki) | Высокая | Среднее | Фокус на AI-agent use case |
| Сложность масштабирования Neo4j | Низкая | Высокое | Архитектура с шардингом |
| Enterprise sales cycle длинный | Высокая | Среднее | PLG + self-serve для SMB |
| Google добавит API для агентов | Средняя | Высокое | Скорость, open protocol, self-hosted |

---

## 🎯 Competitive Analysis

### Карта рынка

```
                    ┌─────────────────────────────────────────────────────┐
                    │                  ДЛЯ ЛЮДЕЙ                          │
                    │                                                     │
                    │   ┌─────────────┐         ┌─────────────────┐       │
                    │   │   GitHub    │         │  Google Code    │       │
                    │   │   Copilot   │         │     Wiki        │       │
                    │   └─────────────┘         └─────────────────┘       │
                    │                                                     │
   ПОНИМАНИЕ        │                                                     │   НАВИГАЦИЯ
   КОДА             │                                                     │   ПО КОДУ
                    │                                                     │
                    │   ┌─────────────┐         ┌─────────────────┐       │
                    │   │ Codex Aura  │         │   Sourcegraph   │       │
                    │   │   (MCP)     │         │                 │       │
                    │   └─────────────┘         └─────────────────┘       │
                    │                                                     │
                    │                  ДЛЯ AI-АГЕНТОВ                     │
                    └─────────────────────────────────────────────────────┘
```

### Детальное сравнение конкурентов

#### 1. Google Code Wiki (Главный конкурент)

| Аспект | Google Code Wiki | Codex Aura |
|--------|------------------|------------|
| **Запуск** | Ноябрь 2025 | — |
| **Статус** | Public preview | В разработке |
| **Фокус** | Документация для людей | Контекст для AI-агентов |
| **Интерфейс** | Web UI + Gemini Chat | REST/gRPC API + SDK |
| **Выход** | Wiki-страницы, диаграммы | Структурированный JSON-граф |

**Что есть у Google:**
- ✅ Автогенерация документации
- ✅ Архитектурные диаграммы (auto-updated)
- ✅ Gemini-powered чат по коду
- ✅ Бренд и дистрибуция
- ✅ Бесплатно для public repos

**Чего НЕТ у Google (наши преимущества):**
- ❌ API с token budgeting для агентов
- ❌ Impact Analysis ("что сломается?")
- ❌ Программный доступ к графу связей
- ❌ Self-hosted вариант
- ❌ Feedback loop от агентов
- ❌ Приоритизация контекста по релевантности
- ❌ **Code Intelligence Layer (валидация кода)**
- ❌ **Business Rules Engine (проверка бизнес-логики)**
- ❌ **Style Guide Extraction (автоизвлечение стиля)**
- ❌ **Pattern Detection (обнаружение паттернов)**

**Стратегия против Google:**
1. **Не конкурировать в UI** — они всегда сделают красивее
2. **Фокус на API** — мы для агентов, они для людей
3. **Enterprise self-hosted** — Google не отдаст
4. **Open Protocol** — community adoption до того, как Google закроет рынок

#### 2. Sourcegraph

| Аспект | Sourcegraph | Codex Aura |
|--------|-------------|------------|
| **Фокус** | Code search & navigation | Контекст для AI-агентов |
| **Сильные стороны** | Поиск по огромным кодовым базам | Семантический граф + Impact |
| **Cody AI** | Да, но generic контекст | Оптимизированный под задачу |
| **Цена** | $$ (enterprise) | Freemium + affordable |

**Наше преимущество:**
- Sourcegraph даёт "всё что нашёл", мы даём "минимум нужного"
- У нас Impact Analysis из коробки
- Дешевле и проще для SMB

#### 3. GitHub Copilot / Copilot Workspace

| Аспект | GitHub Copilot | Codex Aura |
|--------|----------------|------------|
| **Тип** | AI-ассистент в IDE | Инфраструктура для агентов |
| **Контекст** | Текущий файл + соседи | Весь граф проекта |
| **API** | Нет (только UI) | Да, API-first |

**Наше позиционирование:**
- Мы не конкурент Copilot — мы **инфраструктура**
- Copilot мог бы использовать наш API для лучшего контекста
- Партнёрство > конкуренция

#### 4. Другие игроки

| Продукт | Что делает | Почему не конкурент |
|---------|------------|---------------------|
| **Cursor** | AI IDE | UI-продукт, не инфраструктура |
| **Codeium** | Code completion | Нет графа зависимостей |
| **Tabnine** | Code completion | Локальный контекст только |
| **Bloop** | Semantic code search | Нет Impact Analysis |

### Наше уникальное позиционирование

```
┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│   "Codex Aura — это НЕ ещё один AI-ассистент.                     │
│    Это инфраструктура, которая делает ЛЮБОГО                       │
│    AI-агента умнее, давая ему правильный контекст."                │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

**Ключевые дифференциаторы:**

| # | Дифференциатор | Почему важно |
|---|----------------|--------------|
| 1 | **Token Budgeting** | Экономия 70%+ на API-вызовах к LLM |
| 2 | **Impact Analysis** | Единственный инструмент, отвечающий "что сломается" |
| 3 | **Code Intelligence** | Проверка на стиль, паттерны и бизнес-правила |
| 4 | **Agent Feedback Loop** | Граф улучшается от использования |
| 5 | **Business Rules Engine** | AI-агент знает доменные ограничения |
| 6 | **Open Protocol** | Vendor lock-in невозможен |
| 7 | **Self-Hosted** | Enterprise compliance |

### Сценарии конкурентного давления

| Сценарий | Вероятность | Наш ответ |
|----------|-------------|-----------|
| Google добавляет API для агентов | 60% | Уже иметь adoption + open protocol |
| Sourcegraph выпускает "Cody Context API" | 40% | Фокус на цене и простоте |
| GitHub встраивает граф в Copilot | 50% | Self-hosted + multi-provider support |
| Open source альтернатива появляется | 70% | Мы сами open source (core) |

### Go-to-Market против конкурентов

**Phase 1-2: Избегание прямой конкуренции**
- Позиционирование: "Для AI-агентов" (не для людей)
- Канал: AI/ML community, agent builders
- Messaging: "Make your agent smarter, not just faster"

**Phase 3-4: Расширение позиционирования**
- Добавить UI для людей (но API остаётся core)
- Enterprise features (SSO, audit, compliance)
- Партнёрства с AI-платформами

**Phase 5: Platform play**
- Стать стандартом для "code context"
- Интеграции с Copilot, Cursor, Cody как провайдер контекста