# 📋 Фаза 0: Proof of Concept — Детальный План Задач

**Длительность:** 2-3 недели
**Цель:** Валидация идеи и базовой архитектуры
**Результат:** Работающий CLI-прототип, демонстрирующий ценность графового подхода

---

## 📊 Обзор эпиков

| # | Эпик | Задач | Оценка | Приоритет |
|---|------|-------|--------|-----------|
| E1 | Инфраструктура проекта | 6 | 4h | P0 |
| E2 | Спецификация графа | 5 | 6h | P0 |
| E3 | Python Analyzer | 8 | 16h | P0 |
| E4 | CLI Tool | 6 | 8h | P0 |
| E5 | Demo Script | 4 | 4h | P0 |
| E6 | Тестирование | 5 | 8h | P1 |
| E7 | Документация | 4 | 4h | P1 |
| | **ИТОГО** | **38** | **~50h** | |

---

## E1: 🏗️ Инфраструктура проекта

### E1-1: Создание репозитория
**Оценка:** 30 мин | **Приоритет:** P0 | **Зависимости:** —

**Описание:**
Создать GitHub репозиторий с базовой структурой для Python-проекта.

**Действия:**
- [ ] Создать репозиторий `codex-aura` на GitHub
- [ ] Добавить `.gitignore` для Python
- [ ] Добавить `LICENSE` (Apache 2.0)
- [ ] Создать базовую структуру директорий

**Структура:**
```
codex-aura/
├── src/
│   └── codex_aura/
│       ├── __init__.py
│       ├── analyzer/
│       ├── cli/
│       └── models/
├── tests/
├── examples/
├── docs/
├── pyproject.toml
├── README.md
└── LICENSE
```

**Критерии приёмки:**
- [ ] Репозиторий создан и доступен
- [ ] Структура директорий соответствует плану
- [ ] LICENSE файл присутствует

---

### E1-2: Настройка Python-окружения
**Оценка:** 30 мин | **Приоритет:** P0 | **Зависимости:** E1-1

**Описание:**
Настроить современное Python-окружение с pyproject.toml.

**Действия:**
- [ ] Создать `pyproject.toml` с метаданными проекта
- [ ] Настроить Poetry или pip-tools для управления зависимостями
- [ ] Указать минимальную версию Python (3.11+)
- [ ] Добавить базовые зависимости

**pyproject.toml (минимум):**
```toml
[project]
name = "codex-aura"
version = "0.1.0"
description = "Code context mapping for AI agents"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff", "mypy"]

[project.scripts]
codex-aura = "codex_aura.cli.main:main"
```

**Критерии приёмки:**
- [ ] `pip install -e .` работает без ошибок
- [ ] Команда `codex-aura --help` доступна после установки

---

### E1-3: Настройка линтера и форматтера
**Оценка:** 30 мин | **Приоритет:** P1 | **Зависимости:** E1-2

**Описание:**
Настроить Ruff для линтинга и форматирования кода.

**Действия:**
- [ ] Добавить конфигурацию Ruff в `pyproject.toml`
- [ ] Настроить правила (совместимые с Black + isort)
- [ ] Добавить pre-commit hook (опционально)

**Конфигурация:**
```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
```

**Критерии приёмки:**
- [ ] `ruff check src/` проходит без ошибок
- [ ] `ruff format src/` форматирует код

---

### E1-4: Настройка тестового фреймворка
**Оценка:** 30 мин | **Приоритет:** P1 | **Зависимости:** E1-2

**Описание:**
Настроить pytest для запуска тестов.

**Действия:**
- [ ] Добавить pytest в dev-зависимости
- [ ] Создать `tests/__init__.py`
- [ ] Создать `tests/conftest.py` с базовыми фикстурами
- [ ] Добавить конфигурацию pytest

**Конфигурация:**
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
```

**Критерии приёмки:**
- [ ] `pytest` запускается без ошибок
- [ ] Пустой тест проходит успешно

---

### E1-5: Настройка CI (GitHub Actions)
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E1-3, E1-4

**Описание:**
Базовый CI pipeline для проверки кода на каждый PR.

**Действия:**
- [ ] Создать `.github/workflows/ci.yml`
- [ ] Настроить job для линтинга
- [ ] Настроить job для тестов
- [ ] Тестировать на Python 3.11 и 3.12

**Workflow:**
```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: ruff check src/
      - run: pytest
```

**Критерии приёмки:**
- [ ] CI запускается на push и PR
- [ ] Все jobs проходят на чистом коде

---

### E1-6: Создание тестовых репозиториев
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** —

**Описание:**
Подготовить тестовые Python-проекты для валидации анализатора.

**Действия:**
- [ ] Создать `examples/simple_project/` — 5-10 файлов, линейные импорты
- [ ] Создать `examples/flask_mini/` — мини Flask-приложение
- [ ] Клонировать реальный проект для финального теста (Flask/FastAPI)

**simple_project структура:**
```
examples/simple_project/
├── main.py          # imports: utils, services.user
├── utils.py         # imports: config
├── config.py        # no imports
└── services/
    ├── __init__.py
    ├── user.py      # imports: utils, models.user
    └── models/
        └── user.py  # imports: config
```

**Критерии приёмки:**
- [ ] Минимум 2 тестовых проекта готовы
- [ ] Известен "правильный" граф для каждого (для сравнения)

---

## E2: 📐 Спецификация графа (JSON Schema)

### E2-1: Определение модели Node
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** —

**Описание:**
Определить структуру узла графа для Phase 0 (минимальный набор полей).

**Поля Node (v0.1):**

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | string | ✅ | Уникальный идентификатор (path + type + name) |
| `type` | enum | ✅ | `file`, `class`, `function` |
| `name` | string | ✅ | Имя сущности |
| `path` | string | ✅ | Путь к файлу относительно корня |
| `lines` | [int, int] | ❌ | Начальная и конечная строка |
| `docstring` | string | ❌ | Документация (если есть) |

**Пример:**
```json
{
  "id": "src/auth/jwt.py::function::validate_token",
  "type": "function",
  "name": "validate_token",
  "path": "src/auth/jwt.py",
  "lines": [45, 89],
  "docstring": "Validates JWT token and returns claims"
}
```

**Критерии приёмки:**
- [ ] Модель описана в документации
- [ ] Pydantic-модель создана в `src/codex_aura/models/node.py`

---

### E2-2: Определение модели Edge
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** —

**Описание:**
Определить структуру ребра графа для Phase 0 (только IMPORTS).

**Поля Edge (v0.1):**

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `source` | string | ✅ | ID исходного узла |
| `target` | string | ✅ | ID целевого узла |
| `type` | enum | ✅ | `IMPORTS` (в Phase 0 только этот) |
| `line` | int | ❌ | Строка, где происходит импорт |

**Пример:**
```json
{
  "source": "src/auth/jwt.py::file",
  "target": "src/utils/crypto.py::file",
  "type": "IMPORTS",
  "line": 3
}
```

**Критерии приёмки:**
- [ ] Модель описана в документации
- [ ] Pydantic-модель создана в `src/codex_aura/models/edge.py`

---

### E2-3: Определение модели Graph
**Оценка:** 30 мин | **Приоритет:** P0 | **Зависимости:** E2-1, E2-2

**Описание:**
Определить корневую структуру графа.

**Поля Graph (v0.1):**

```json
{
  "version": "0.1.0",
  "generated_at": "2024-01-15T10:30:00Z",
  "repository": {
    "path": "/path/to/repo",
    "name": "my-project"
  },
  "stats": {
    "total_nodes": 42,
    "total_edges": 67,
    "node_types": {"file": 15, "class": 12, "function": 15}
  },
  "nodes": [...],
  "edges": [...]
}
```

**Критерии приёмки:**
- [ ] Pydantic-модель `Graph` создана
- [ ] Модель включает metadata (version, timestamp)

---

### E2-4: Создание JSON Schema файла
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E2-3

**Описание:**
Экспортировать Pydantic-модели в JSON Schema для публичной спецификации.

**Действия:**
- [ ] Использовать `model.model_json_schema()` для генерации
- [ ] Сохранить в `docs/schema/graph-v0.1.json`
- [ ] Добавить описания полей в schema

**Критерии приёмки:**
- [ ] JSON Schema файл валиден
- [ ] Можно валидировать JSON-граф против schema

---

### E2-5: Реализация сериализации/десериализации
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E2-3

**Описание:**
Функции для сохранения и загрузки графа из JSON.

**API:**
```python
# src/codex_aura/models/graph.py

def save_graph(graph: Graph, path: Path) -> None:
    """Save graph to JSON file."""
    
def load_graph(path: Path) -> Graph:
    """Load graph from JSON file."""
```

**Критерии приёмки:**
- [ ] Round-trip: save → load → save даёт идентичный результат
- [ ] Ошибки валидации понятны пользователю

---

## E3: 🔍 Python Analyzer

### E3-1: Базовая структура анализатора
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E2-3

**Описание:**
Создать скелет анализатора с интерфейсом.

**Структура:**
```
src/codex_aura/analyzer/
├── __init__.py
├── base.py          # абстрактный класс
├── python.py        # Python-анализатор
└── utils.py         # вспомогательные функции
```

**Интерфейс:**
```python
# base.py
from abc import ABC, abstractmethod

class BaseAnalyzer(ABC):
    @abstractmethod
    def analyze(self, repo_path: Path) -> Graph:
        """Analyze repository and return graph."""
        pass
    
    @abstractmethod
    def analyze_file(self, file_path: Path) -> list[Node]:
        """Analyze single file and return nodes."""
        pass
```

**Критерии приёмки:**
- [ ] Абстрактный класс определён
- [ ] PythonAnalyzer наследуется от BaseAnalyzer

---

### E3-2: Сканирование Python-файлов
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E3-1

**Описание:**
Рекурсивный поиск всех `.py` файлов в репозитории.

**Функционал:**
- [ ] Рекурсивный обход директорий
- [ ] Фильтрация по `.py` расширению
- [ ] Игнорирование `__pycache__`, `.venv`, `node_modules`
- [ ] Поддержка `.gitignore` паттернов (опционально)

**API:**
```python
def find_python_files(repo_path: Path) -> list[Path]:
    """Find all Python files in repository."""
```

**Критерии приёмки:**
- [ ] Находит все `.py` файлы
- [ ] Игнорирует стандартные директории
- [ ] Возвращает относительные пути

---

### E3-3: Парсинг AST для извлечения узлов (файлы)
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E3-2

**Описание:**
Создание Node типа `file` для каждого Python-файла.

**Действия:**
- [ ] Парсить файл через `ast.parse()`
- [ ] Извлекать docstring модуля (если есть)
- [ ] Создавать Node с типом `file`

**API:**
```python
def parse_file_node(file_path: Path, repo_root: Path) -> Node:
    """Create file node from Python file."""
```

**Критерии приёмки:**
- [ ] Node создаётся для каждого файла
- [ ] Docstring извлекается корректно
- [ ] Обрабатываются файлы с синтаксическими ошибками (skip + warning)

---

### E3-4: Парсинг AST для извлечения классов
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E3-3

**Описание:**
Извлечение всех классов из Python-файла.

**Действия:**
- [ ] Использовать `ast.ClassDef` для поиска классов
- [ ] Извлекать имя, docstring, номера строк
- [ ] Поддержка вложенных классов (опционально)

**API:**
```python
def extract_classes(tree: ast.AST, file_path: Path) -> list[Node]:
    """Extract class nodes from AST."""
```

**Критерии приёмки:**
- [ ] Все top-level классы извлекаются
- [ ] Docstring класса извлекается
- [ ] Номера строк корректны

---

### E3-5: Парсинг AST для извлечения функций
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E3-3

**Описание:**
Извлечение всех функций из Python-файла.

**Действия:**
- [ ] Использовать `ast.FunctionDef` и `ast.AsyncFunctionDef`
- [ ] Различать top-level функции и методы классов
- [ ] Извлекать имя, docstring, номера строк

**API:**
```python
def extract_functions(tree: ast.AST, file_path: Path) -> list[Node]:
    """Extract function nodes from AST."""
```

**Критерии приёмки:**
- [ ] Функции и async-функции извлекаются
- [ ] Методы классов имеют правильный ID (class::method)
- [ ] Docstring извлекается

---

### E3-6: Парсинг импортов (IMPORTS edges)
**Оценка:** 3h | **Приоритет:** P0 | **Зависимости:** E3-3

**Описание:**
Извлечение всех импортов и создание IMPORTS edges.

**Типы импортов:**
```python
import os                      # стандартная библиотека (игнорируем)
import requests               # внешний пакет (игнорируем в Phase 0)
from .utils import helper     # относительный импорт (обрабатываем!)
from src.auth import jwt      # абсолютный внутренний (обрабатываем!)
```

**Действия:**
- [ ] Парсить `ast.Import` и `ast.ImportFrom`
- [ ] Разрешать относительные импорты в абсолютные пути
- [ ] Определять, является ли импорт внутренним
- [ ] Создавать Edge типа IMPORTS

**API:**
```python
def extract_imports(
    tree: ast.AST, 
    file_path: Path,
    repo_root: Path,
    all_files: set[Path]
) -> list[Edge]:
    """Extract import edges from AST."""
```

**Критерии приёмки:**
- [ ] Относительные импорты разрешаются корректно
- [ ] Внешние пакеты игнорируются
- [ ] Несуществующие импорты не создают edges (warning в лог)

---

### E3-7: Сборка полного графа
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E3-4, E3-5, E3-6

**Описание:**
Объединение всех узлов и рёбер в единый граф.

**Действия:**
- [ ] Собрать все nodes из всех файлов
- [ ] Собрать все edges
- [ ] Проверить целостность (все target в edges существуют)
- [ ] Вычислить статистику

**API:**
```python
class PythonAnalyzer(BaseAnalyzer):
    def analyze(self, repo_path: Path) -> Graph:
        """Full repository analysis."""
```

**Критерии приёмки:**
- [ ] Граф содержит все узлы и рёбра
- [ ] Нет "висячих" edges (target не существует)
- [ ] Статистика корректна

---

### E3-8: Обработка ошибок и логирование
**Оценка:** 2h | **Приоритет:** P1 | **Зависимости:** E3-7

**Описание:**
Graceful handling ошибок при анализе.

**Сценарии:**
- [ ] Файл с синтаксической ошибкой → skip + warning
- [ ] Файл недоступен для чтения → skip + warning
- [ ] Циклические импорты → обрабатываются корректно
- [ ] Очень большой файл → предупреждение

**Логирование:**
```python
import logging
logger = logging.getLogger("codex_aura")

# Уровни:
# DEBUG - детали парсинга
# INFO - прогресс (файл N из M)
# WARNING - пропущенные файлы
# ERROR - критические ошибки
```

**Критерии приёмки:**
- [ ] Анализ не падает на невалидных файлах
- [ ] Пользователь видит warnings о пропущенных файлах
- [ ] Есть verbose режим для отладки

---

## E4: 💻 CLI Tool

### E4-1: Базовая структура CLI
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E3-7

**Описание:**
Создать CLI с использованием Click или argparse.

**Структура:**
```
src/codex_aura/cli/
├── __init__.py
└── main.py
```

**Базовые команды:**
```bash
codex-aura --help
codex-aura --version
codex-aura analyze <path>
```

**Критерии приёмки:**
- [ ] `codex-aura --help` показывает справку
- [ ] `codex-aura --version` показывает версию

---

### E4-2: Команда `analyze`
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E4-1

**Описание:**
Основная команда для анализа репозитория.

**Использование:**
```bash
codex-aura analyze /path/to/repo
codex-aura analyze /path/to/repo --output graph.json
codex-aura analyze /path/to/repo --format json|pretty
codex-aura analyze . --verbose
```

**Опции:**
| Опция | Короткая | Описание | Default |
|-------|----------|----------|---------|
| `--output` | `-o` | Путь для сохранения | stdout |
| `--format` | `-f` | Формат вывода | `json` |
| `--verbose` | `-v` | Подробный вывод | False |
| `--quiet` | `-q` | Минимальный вывод | False |

**Критерии приёмки:**
- [ ] Анализирует указанную директорию
- [ ] Выводит JSON в stdout или файл
- [ ] Показывает прогресс при `--verbose`

---

### E4-3: Форматированный вывод
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E4-2

**Описание:**
Красивый вывод статистики после анализа.

**Пример вывода:**
```
✓ Analyzed: my-project
  
  📊 Statistics:
     Files:     15
     Classes:   12
     Functions: 45
     Imports:   67
  
  📁 Output: graph.json (24 KB)
  ⏱  Time: 1.2s
```

**Критерии приёмки:**
- [ ] Статистика выводится после анализа
- [ ] Используются emoji/цвета (если терминал поддерживает)

---

### E4-4: Валидация входных параметров
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E4-2

**Описание:**
Проверка корректности входных данных.

**Проверки:**
- [ ] Путь существует
- [ ] Путь — директория (не файл)
- [ ] Директория содержит Python-файлы
- [ ] Есть права на чтение

**Сообщения об ошибках:**
```
Error: Path '/invalid/path' does not exist
Error: '/path/to/file.py' is not a directory
Error: No Python files found in '/empty/dir'
```

**Критерии приёмки:**
- [ ] Понятные сообщения об ошибках
- [ ] Exit code 1 при ошибках

---

### E4-5: Команда `stats` (опционально)
**Оценка:** 1h | **Приоритет:** P2 | **Зависимости:** E4-2

**Описание:**
Показать статистику существующего графа.

**Использование:**
```bash
codex-aura stats graph.json
```

**Вывод:**
```
Graph: my-project (v0.1.0)
Generated: 2024-01-15 10:30:00

Nodes by type:
  file:     15 (20%)
  class:    12 (16%)
  function: 45 (64%)

Most connected files:
  1. src/utils.py      (12 incoming, 3 outgoing)
  2. src/models/base.py (8 incoming, 1 outgoing)
```

**Критерии приёмки:**
- [ ] Загружает и парсит JSON
- [ ] Показывает полезную статистику

---

### E4-6: Интеграция с pyproject.toml
**Оценка:** 30 мин | **Приоритет:** P0 | **Зависимости:** E4-2

**Описание:**
Убедиться, что CLI устанавливается как entry point.

**pyproject.toml:**
```toml
[project.scripts]
codex-aura = "codex_aura.cli.main:main"
```

**Критерии приёмки:**
- [ ] После `pip install -e .` команда `codex-aura` доступна
- [ ] Работает в любой директории

---

## E5: 🎬 Demo Script

### E5-1: Скрипт "affected files"
**Оценка:** 2h | **Приоритет:** P0 | **Зависимости:** E4-2

**Описание:**
Демонстрация ключевой ценности: какие файлы затронуты при изменении.

**Использование:**
```bash
python examples/demo_affected.py graph.json src/utils.py
```

**Вывод:**
```
🎯 Analyzing impact of changes to: src/utils.py

📥 Files that IMPORT src/utils.py (will be affected):
   • src/services/user.py (line 3)
   • src/services/auth.py (line 5)
   • src/main.py (line 2)

📤 Files that src/utils.py IMPORTS (dependencies):
   • src/config.py

🔄 Total impact: 4 files
```

**Критерии приёмки:**
- [ ] Показывает incoming edges (кто импортирует)
- [ ] Показывает outgoing edges (что импортирует)
- [ ] Понятный вывод для не-технического пользователя

---

### E5-2: Скрипт визуализации (текстовый)
**Оценка:** 1h | **Приоритет:** P2 | **Зависимости:** E5-1

**Описание:**
ASCII-визуализация графа для демонстрации в терминале.

**Вывод:**
```
src/main.py
├── imports: src/utils.py
│   └── imports: src/config.py
├── imports: src/services/user.py
│   ├── imports: src/utils.py
│   └── imports: src/models/user.py
```

**Критерии приёмки:**
- [ ] Древовидный вывод импортов
- [ ] Глубина ограничена (--depth)

---

### E5-3: Benchmark скрипт
**Оценка:** 30 мин | **Приоритет:** P1 | **Зависимости:** E5-1

**Описание:**
Измерение производительности на реальном репозитории.

**Использование:**
```bash
python examples/benchmark.py /path/to/large/repo
```

**Вывод:**
```
Benchmark: flask (127 files, 45K LOC)
  Scan files:    0.05s
  Parse AST:     0.82s
  Build graph:   0.15s
  Total:         1.02s
  
Performance: 44K LOC/sec
```

**Критерии приёмки:**
- [ ] Измеряет время каждого этапа
- [ ] Выводит LOC/sec метрику

---

### E5-4: README с примерами
**Оценка:** 30 мин | **Приоритет:** P0 | **Зависимости:** E5-1

**Описание:**
Обновить README с примерами использования demo-скриптов.

**Секции:**
- Quick Start
- Demo: Impact Analysis
- Demo: Graph Stats
- Benchmark Results

**Критерии приёмки:**
- [ ] README содержит copy-paste примеры
- [ ] Примеры работают на тестовых репозиториях

---

## E6: 🧪 Тестирование

### E6-1: Unit-тесты для моделей
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E2-5

**Тесты:**
- [ ] `test_node_creation` — создание Node
- [ ] `test_edge_creation` — создание Edge
- [ ] `test_graph_serialization` — save/load round-trip
- [ ] `test_invalid_node` — валидация ошибок

**Критерии приёмки:**
- [ ] 100% coverage для models/

---

### E6-2: Unit-тесты для анализатора
**Оценка:** 3h | **Приоритет:** P0 | **Зависимости:** E3-7

**Тесты:**
- [ ] `test_find_python_files` — поиск файлов
- [ ] `test_parse_simple_file` — базовый парсинг
- [ ] `test_extract_classes` — извлечение классов
- [ ] `test_extract_functions` — извлечение функций
- [ ] `test_extract_imports` — извлечение импортов
- [ ] `test_relative_imports` — относительные импорты
- [ ] `test_syntax_error_handling` — обработка ошибок

**Критерии приёмки:**
- [ ] Все тесты проходят
- [ ] Coverage > 80% для analyzer/

---

### E6-3: Integration-тесты
**Оценка:** 2h | **Приоритет:** P1 | **Зависимости:** E6-2

**Тесты:**
- [ ] `test_analyze_simple_project` — полный анализ тестового проекта
- [ ] `test_analyze_flask_mini` — анализ Flask-приложения
- [ ] `test_cli_analyze` — CLI команда analyze

**Критерии приёмки:**
- [ ] Граф для тестовых проектов соответствует ожидаемому
- [ ] CLI работает end-to-end

---

### E6-4: Тесты на реальных проектах
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E6-3

**Репозитории для теста:**
- [ ] Flask (~50K LOC) — должен проанализироваться < 10 сек
- [ ] FastAPI (~30K LOC) — должен проанализироваться < 5 сек
- [ ] Requests (~10K LOC) — должен проанализироваться < 2 сек

**Критерии приёмки:**
- [ ] Все репозитории анализируются без ошибок
- [ ] Время соответствует целевым метрикам

---

### E6-5: Тесты на edge cases
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E6-2

**Edge cases:**
- [ ] Пустой репозиторий
- [ ] Один файл без импортов
- [ ] Циклические импорты (A → B → A)
- [ ] Файл с только комментариями
- [ ] Файл с encoding declaration
- [ ] Файл > 10K строк

**Критерии приёмки:**
- [ ] Все edge cases обрабатываются gracefully

---

## E7: 📚 Документация

### E7-1: README.md
**Оценка:** 1h | **Приоритет:** P0 | **Зависимости:** E5-4

**Секции:**
- [ ] Badges (CI, version, license)
- [ ] Краткое описание (что и зачем)
- [ ] Installation
- [ ] Quick Start
- [ ] Usage Examples
- [ ] Contributing
- [ ] License

**Критерии приёмки:**
- [ ] README достаточен для начала работы
- [ ] Все примеры работают

---

### E7-2: API Documentation (docstrings)
**Оценка:** 1h | **Приоритет:** P1 | **Зависимости:** E3-7

**Требования:**
- [ ] Все публичные функции имеют docstrings
- [ ] Формат: Google style
- [ ] Типы указаны в сигнатурах (type hints)

**Критерии приёмки:**
- [ ] `pydoc codex_aura` показывает документацию

---

### E7-3: CHANGELOG.md
**Оценка:** 30 мин | **Приоритет:** P1 | **Зависимости:** —

**Формат:** Keep a Changelog

```markdown
# Changelog

## [0.1.0] - 2024-01-20

### Added
- Initial release
- Python analyzer with IMPORTS support
- CLI tool with `analyze` command
- JSON graph format v0.1
```

**Критерии приёмки:**
- [ ] CHANGELOG создан
- [ ] Формат соответствует стандарту

---

### E7-4: CONTRIBUTING.md
**Оценка:** 30 мин | **Приоритет:** P2 | **Зависимости:** —

**Секции:**
- [ ] How to contribute
- [ ] Development setup
- [ ] Code style
- [ ] Pull request process

**Критерии приёмки:**
- [ ] Новый контрибьютор может начать работу по гайду

---

## ✅ Definition of Done (Phase 0)

Фаза 0 считается завершённой, когда:

- [ ] CLI `codex-aura analyze <path>` работает
- [ ] JSON-граф генерируется для Python-проектов
- [ ] Demo-скрипт показывает affected files
- [ ] Тесты проходят на CI
- [ ] README позволяет начать работу
- [ ] Анализ Flask (~50 файлов) занимает < 10 сек
- [ ] Код отформатирован и проходит линтер